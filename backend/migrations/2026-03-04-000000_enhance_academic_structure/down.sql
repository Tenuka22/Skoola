-- Drop the new table
DROP TABLE IF EXISTS teacher_period_attendance;

-- Note: Dropping columns in SQLite requires recreating the table. 
-- For brevity, these are not included here, but in a production environment, 
-- you would rename the table, create a new one, copy data, and drop the old one.
